<!-- 
1. 不阻塞日常开发：严格模式落地和业务迭代并行，不要求一次性修复所有报错；
2. 渐进式治理：按 “文件 / 模块优先级” 分阶段修复，先核心业务，后边缘模块；
3. 工具兜底：用 TS 配置 + ESLint 规则减少手动修复成本，避免重复劳动；
4. 流程保障：新增代码必须符合严格模式，老代码逐步收敛，防止 “新债旧账一起欠”。 
-->

调整 tsconfig.json：分模块开启严格模式（而非全局）
临时兜底：对核心报错文件添加 @ts-nocheck
新增类型声明兜底（减少 “找不到类型” 报错）

分阶段修复报错，从 “新代码” 到 “老代码”
逐步开启 strict 子选项（最后开启严格 Null 检查）
全量开启 strict: true
工具链保障（ESLint+CI/CD）
